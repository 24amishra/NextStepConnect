rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Contract PDFs - only admins can upload, authenticated users can read
    match /contracts/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
